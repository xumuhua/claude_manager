# D-L1 wau_top 分层试点·L1 核心层——生成实录

日期：2026-09-03 · 执行：coder（kimi-han）· 预算 4h · 方法：ir_book part6 §14.2.4 试标（7ac43b7）首发试点

## 1. L1 范围（一句话）

wau_top 整模块 L1 核心层：single/multi 两型 UOP 的接受→chunk 几何→bank 请求派发→免 tag 映射登记→回数落槽→信用闭环→rack 收集→致密/移位拼线→保序 128B/拍出线全数据通路骨架；trans/sync/事件存续钉/ready 寄存化等 L2/L3 语义不实现（12 处扩展点注释留位）。

## 2. 规格权威与禁读合规

- 权威：chip_design_ir ir-refactor `examples_vnext/wau_top/` 五件套 + hlc/（D2B 9929fa1 + 后续回补，7ac43b7 HEAD）。
- 禁读执行：真品 RTL（rtl_gen/）、A/B/C/D 各组 WAU 实现、旧 examples/、splitter 生成物——全程零读取（splitter 仅携经验、零代码照抄）。

## 3. §13 五约束自检实录（写实录非口号）

| 约束 | 机检形态 | 结果 |
|---|---|---|
| G-SV-TYPE 纯 Verilog | grep `\b(logic\|always_ff\|always_comb\|typedef\|interface\|package)\b` 四 RTL 文件 | 零命中 ✓ |
| G-COMB-STAR 组合禁 always@(*) | grep `always\s*@(\*)` | 零命中 ✓ |
| G-COMB-CASE 全文件禁 case | grep `\bcase[xz]?\b` | 零命中（仅注释提及"禁 case"字样）✓ |
| G-FUNC 禁 function/task | grep `\bfunction\b\|\btask\b` | 零命中（仅注释提及；TB 侧豁免）✓ |
| G-HS-MARK 流水标注 | 固定标记 `// 【流水模式：…】` | 11 处标记分布四文件（top3/split5/retbuf1/asm2），握手集中区注释随标记落位 ✓ |

另：generate 仅参数展开分支（逐 bank/逐字节/逐线展开，无循环逻辑外语义）；iverilog -g2005 -Wall 全链编译**零告警**。

## 4. 层判卷自检结果（交付前已跑）

1. **RTL 零告警**：`iverilog -g2005 -Wall` 四 RTL + TB 全链零告警 ✓
2. **core 契约案逐点比对**（期望=contract.ir 钉值，与判卷棒 densify_abs 同式复核）：
   - c_xuop_b2b_data：3 拍 data_out 逐字节 + strb + rack 序(160→177) **ALL MATCH** ✓（sim_b2b.log）
   - c_single_window_edge：1 拍 data_out + strb(0x…0fff…00ff) + rack(208) **ALL MATCH** ✓（sim_edge.log）
3. **core 不变式 SVA 子集**（iverilog 无 SVA 语法，以拍后探针+违例计数等价落地，tb_sva.v 挂 b2b 真实流量）：
   - INV1 在途 beat ≤ POOL(16)（信用闭环）✓
   - INV2 uop 握手 stall 期载荷稳定 ✓
   - INV3 data 出线 line_seq 保序 ✓
   - INV4 rack mid 序 = uop 接受序 ✓
   - INV5 无幽灵流量（首 uop 接受前出口恒 0）✓
   - 判定：**SVA ALL PASS (5 invariants)**

注：本自检不构成判卷——manager 派 glmdev 独立层判卷为准。

## 5. 扩展点清单（12 处）

- wau_top.v:13 `:L3-READY-CUT`（uop_in/data_out/bank_data 三 ready 路径寄存化）
- wau_top.v:16 `:L2-SYNC`（sync 入口拦停+屏障判定——L1 拦停 sync 于口）
- wau_top.v:168 外口透传段 `:L3-READY-CUT` 落点
- wau_split.v:13-15 `:L2-TRANS`（trans 对角读几何）+ G120 同 beat 同 bank 2 chunk
- wau_split.v:326-327 sync 集合制屏障挂接点 / G123 存续钉入 L3
- wau_retbuf.v:15-17 `:L3-G123`（事件存续）/ `:L3-G110`（同拍多完成串行化）
- wau_retbuf.v:277 `:L3-G110` 完成沿串行化代偿注记
- wau_asm.v:14 `:L2-TRANS-ASM`（trans 槽对合并段，head_flat 16 槽并口已留足）

## 6. 关键实现裁定与病史（供后续层/判卷参考）

1. **occ 位图聚合写**（iverilog 病史⑨）：同拍同数组不同下标多次 NBA 写只执行最后一次——落槽位图须先组合聚合成 occ_next 再单次写；完成沿判定同理由 per-bank 旧值差分改为 line 级聚合差分（同拍 3 路落槽补满时 per-bank 判定恒不成立——R177 漏发即此因）。
2. **strb 致密头部压缩口径**：densify_abs 钉值反推——回挂前缀组字节致密入 data 但不占 strb 位，strb = 半区头部 n_lo/n_hi 连续位图（edge 案 0x00ff 非 0x0ff0）；asm_line.hlc:39 组计数公式是 rot=0 整组形态，n_lo/n_hi 逐字节计数为其 rot≠0 推广，rot=0 两口径等价。
3. **data 组序位选**：高半区组序 j 须取 gb[5:2]（剔除半区位 gb[6]），gb[6:2] 会把 j 抬高 16（高半区基址全错病史）。
4. **bm_push 开户 occ 清零**：line 复用（line_seq 回卷）时旧位图须清零，开户优先级高于 pop 聚合写（开户 line 无在飞请求，同拍同 line 不可能）。
5. TB 侧病史（供判卷环境参考）：驱动/采样须同窗（`@(posedge clk); #1;` 同行，分两行则 valid 挂两拍致双 pop）；粘性 req_seen 位图解决门闩错过已发请求；数组拼接索引 `mem[{a,b}]` 读 x，须整型算术 `mem[a*16+b]`。

## 7. 交付清单

- `rtl/`：wau_top.v / wau_split.v / wau_retbuf.v / wau_asm.v（纯 Verilog-2005）
- `flow.ir`：§13.7 JSON+topo 形态，§14.3.3 层分组（group:"L1"），只覆盖 L1 数据通路/流水级/握手点（10 节点/9 边/5 握手点/2 流水模式）
- `topo.txt`：flow.ir 伴生文本视图（权威以 JSON 为准）
- `tb_l1.v`：core 两案判卷 TB（+CASE=b2b|edge，日志 $fopen 固定名 sim_l1.log）
- `tb_sva.v`：core 不变式 5 条自挂 TB
- `sim_b2b.log` / `sim_edge.log`：两案仿真事务日志（与契约钉值逐点比对用）
- `生成实录.md`：本文件
