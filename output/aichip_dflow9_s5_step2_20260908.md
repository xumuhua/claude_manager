# D-FLOW9 阶段五 Step2 摘要（2026-09-08）

哥哥好，Step2 两方向已按令实施完毕/止于红线，汇报如下。

## 方向一：A 案 credit 回收点前移——完成 ✅

- **IR 四件先行**（behavior/perf/module/flow）：在途语义缩为「已开户未完成」，已齐未出线由 DONE_Q（深=POOL）承接排队不占信用；出口保序契约零改动（iface/contract 未动，top_check 原件直过）。flow_check 11 PASS + B1s 5 PASS。
- **RTL 五处** + 一个**实施中发现的既有潜伏缺陷修复**（如实报告）：cov 完成判定原本只挂在「回数落槽拍」，G110 串行化下同拍双线收齐会丢事件 → credit 缺口 → 死锁（首跑 TIMEOUT 铁证：发射 87 vs 完成入队 71，缺 16=inflight 顶满）。原实现不炸是信用反压把回数密度压稀掩盖了它；A 案解除反压后必现。修法=完成判定补「已齐未入队」残留源（每拍仍恰 ≤1 条，G110/G123 不变）+ 出线资格加 DONE_Q 入队门。**裁定为 RTL 既有缺陷被暴露，非提案语义错误，不改方案方向**。
- **验收全过**：三 case RESULT_SUCCESS 0 ERROR（std 5,789→5,651 / basic 1,750→1,675 / pro 5,789→5,651）；sweep 复测六值×三 case 18/18 PASS；B4 六节点 ALL PASS；B7 复跑 50 mismatch 与基线同族同量（仅相位平移）；VCD 三 case 落盘。
- **与预期偏差如实报告**：std 实测 5,651，低于提案预期 2,400–2,900 与保守下限 4,373。VCD 占拍分解根因：①序卡空闲 1,416 拍→≈0（A 案语义兑现 ✓）；②DONE_Q 本就不积压（{0:1094,1:1093}）——「已齐→出线」延迟本来就 ~1 拍/线，可提前释放的信用窗极窄（138 拍降幅与账自洽）；③剩下 3,320 拍空闲是「无齐空闲」：在途 15/16 顶满、占住池子的是**未齐慢线**（bank 方差），回收点再早也收不出 credit——这正是 Step1 备查「p64 nocredit=0 仍 30.7% 无齐空闲=bank 方差主导」的预言。④POOL≥32 反而劣化（p64 basic +13%）：完成串行化（每拍 1 条）+入队门在信用充沛区成为新税。**结论：A 案在信用受限区（POOL≤24）稳定小幅改善（−1%~-6%），POOL=16 现役点净收益 −138 拍（−2.4%）；大幅降拍的钥匙不在回收点，在 bank 方差缓冲（未齐窗口加深）**。

## 方向二：bank 域 barrel 旋转落线——⚠️ 红线触发，RTL 未动，待裁定

IR 登记三件已完成（behavior df_ret_collect 旋转子段 / perf barrel 预算+面积注记 / flow slot_in 路由注记），flow_check 回归过。但 RTL 实施前的全量核查发现**提案三处结构性前提与实测冲突**：

1. **统一译码式对 linear 不成立**：提案断言「linear 恒等折叠 (s>>1)+(s&1)=s」数学上 s≥2 即假。linear 真式是 lane=(k0+s)&31（+1 步进），与 trans 双带式不同构。三 case VCD 实测：24,070 实例 fit（全是 trans），**351 实例 nofit（全是 linear）**。Step1「全量自检 True」的验证口径存疑（自检脚本未存档，iter_log 已如实登记）。
2. **x(s) 非 single 射**：s=2,3→x=1 撞列，「位置 x 写列 s」有 2:1 歧义（map FIFO 无随行相位位可消歧）。
3. **单共享 barrel 前提不成立**：同拍回数 96% 跨 2–12 行（k0 各异）并存——「bank_data 总线旋 -k0」结构上无法路由，除非按行分组多 barrel（面积收益打折）或回数重排（动拍数，违「纯数据通路」验收口径）。

按红线 1「发现提案与现实冲突停下上报不自行改方案」执行。**待哥哥裁定**：①按新证据重制提案（分模式译码/行分组 barrel/每 lane 小旋转等替代）；②方向二挂起归后续棒；③IR 已登记的旋转子段去留（已加实证修订标注，不撤回待令）。RTL 保持方向一收官态。

## 其他

- 方向一的 B7 err 计数上升（6105→25246）系 golden v6 语义未随 A 案更新 + 比对窗口变化（50 条 mismatch 节点/首差位分布与基线完全同族），golden 模型更新归后续棒，iter_log 已登记。
- 交付：iter_log perf-7 Step2 全记录（含死锁根因链 + 方向二冲突证据链）；sweep5 18/18；VCD 归档；ir-refactor 分支 push（本次 commit）。

—— aichip 2026-09-08
