# D-FLOW9 阶段四 Step2 完成摘要：出线侧槽位参数化落地（含跨回卷同余两修）

哥哥，Step2 已收官：**sweep4 全域 18/18 PASS、三 case 拍数随 POOL 单调递减**，验收口径全部达成。过程中挖出并修掉两个 Step1 提案未预见的真根因（非 2 幂 POOL 的两类失败），登记如下。

## 一、拍板结论落地
1. **POOL 上界 64**：module.ir `le 32 → le 64`（desc 消歧：旧约束 32 与 RTL 注释 64 打架，统一为 64；扫参域 48/64 合法化）。
2. **DONE_Q 深度 = POOL**：G123 语义直接推出（完成事件 ≤ 在途线数 ≤ POOL），未新增独立参数。

## 二、交付清单
| 项 | 结果 |
|---|---|
| IR 四件 | commit `8dc60f9`（module/behavior/perf 按 Step1 §2 逐条 + flow.ir 随动注记） |
| RTL wau_top.v | commit `0536e87`（Step1 §1 十二点 + 实证两修 + behavior 补注） |
| B1s 严格版 | 5 PASS（15 边 × 69 信号） |
| B4 六节点 SVA | ALL PASS |
| B7 golden | 复跑 50 mismatch 与 S3 基线 diff 为空（POOL=16 既有结论适用） |
| 块一 flow_check | 11 PASS |
| 三 case | RESULT_SUCCESS / 0 DATA / 0 STRB，VCD 全落盘 |

## 三、扫参正式表（修后全量重扫，18/18 PASS）
| POOL | std | basic | pro |
|-----:|----:|------:|----:|
| 8 | 8880 | 2708 | 8879 |
| 16 | 5789 | 1750 | 5789 |
| 24 | 4529 | 1437 | 4599 |
| 32 | 3799 | 1221 | 3754 |
| 48 | 3615 | 1133 | 3589 |
| 64 | 3353 | 1055 | 3284 |

- **拍数随 POOL 全域单调递减 ✓**（验收口径达成）。
- POOL=16 拍数 5789/1750/5789 **与基线逐拍一致**（互锁在 2 幂下位等价实证，回归无扰动）。
- POOL=8 **历史上首次全 PASS**（S3 死锁根因「选择树 16 候选写死」已随参数化消除）。
- S3 拐点结论维持：64 vs 32 仅省 ~10-12%，边际递减，默认值 16 不动。

## 四、两个新根因（Step1 提案的安全域分析盲区，已修+已回写 IR 注记）
### 修 1+2：非 2 幂 POOL（24/48）两级根因
- **第一层（提案 §4 已预警的变体）**：五处 `seq[LB_W-1:0]` 位截断 = **mod 2^LB_W ≠ mod LB**——POOL=24 时 mod32，seq24..31 寻址到 [0:23] 数组外。改真取模 `% LB`（常量除法可综合，2 幂自动优化回截断）。
- **第二层（全新发现）**：取模修对后 24/48 仍 FAIL。DBGAST 撞槽断言实测 9 撞锁定：line_seq 8b **mod 256 回卷**，`256 mod 24 = 16 ≠ 0`——在途窗跨回卷边界时，第二圈 seq 与第一圈尾构成**环序差 16 的同余对**（240↔0 等）。IR『在途窗 < 模数 ⇒ 同余不可能』的直证**仅在窗不跨回卷边界时成立**；2 幂 POOL（256 mod POOL = 0）跨界同余对环序距离 ≥ POOL 恒出窗、结构性无撞——与旧码扫参 PASS/FAIL 全域吻合（8/16/32 过、24/48 炸）。
- **修法**：发射拍槽占用互锁 `credit_avail &= ~lb_valid[seq mod LB]`——同余复用硬性推迟到旧线 P1 fire 后。POOL 语义与吞吐主路径不变（跨界窗 ≤16 拍、每圈一次，性能损失可忽略；POOL=16 拍数逐拍一致实证）。
### 修 3：POOL=64 pro FAIL
- `beat_inflight [5:0]` 计到 63 后 +1 **溢出回 0** → credit_avail 恒真、信用闭环失效。改 `[6:0]` 7b 全域。
### IR 回写
behavior.ir df_ret_collect/uniqueness 补「跨回卷同余缺口」注记（实证修正，语义不变）——IR 论证与 RTL 实现的对齐缺口闭环。

## 五、诊断资产（可复用）
- `tb_dbg.v`：DBGU 扩窗（bif 全程）+ 撞槽断言 DBGAST1/2（开户撞占用槽/同拍同槽）+ 计数探针 DBGCNT（meta/dout/lfire/bfire/cret 五账对平验证）。
- 根因链完整证据：出线序 239→247 跳 8 线 → 槽表 seq 覆盖实测（241↔1 …）→ 数学判据 `256 mod LB ≥ LB-1` 判安全域。

## 六、红线遵守
env_l1/top_check 未动；sim 全程 VCD；串行单进程；A/B/C/D 组隔离；IR+RTL 改动逐条对标 Step1 提案，两处实证修均登记 iter_log（perf-6 段）。

下一步建议（等拍板）：S3 拐点 + S4 全域表已齐，POOL 默认 16 维持；若需进一步压拍数，POOL=32 是性价比档（-34% 拍数/+4KB 面积）。
