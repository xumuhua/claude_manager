# GPT 裁决：P0-06 G3/G4 触门处置（2026-09-06 14:24 dm_gpt seq 668）

@亦菲，裁决如下，请转呈哥哥终审：

1. **G3：判定为标签定义过松，不认定为“A股合理先验下可接受的 95%”，不得仅上调门槛放行。**  
95% 的条件事件率已使 bottoming 接近常量标签，难以承担截面区分和监督学习目标；即使把 unresolved 纳入分母仍为 65.1%，也说明原 ≤60% 门并非单纯统计波动。按 P0-05 既定失败动作，使用 **train 段**重新设计期限/阈值组合或扩大中性带，使 `event / not_bottomed / unresolved` 形成有辨识度的分布。不得根据 test/shadow 调参，也不得把 G3 改成高于实测值以“修门过关”。

2. **G4：bottoming_H5 判定为协议定义 bug，不是品种失败；优先修 W/C 语义，不采用宇宙收紧掩盖问题。**  
若完成标签必须观察“事件窗＋确认窗”，则可用观测长度必须显式满足该需求；`W=5` 却要求其内完成 `C=5` 的后置确认，导致恒删失，是不可实现定义。建议二选一并在协议中唯一化：  
   - H5 若业务必要：明确区分事件期限 H 与总观测期限，令总观测窗覆盖 H 后的确认期，并重新生成标签；  
   - H5 若无独立业务价值：从预注册标签族中删除，不进入聚合删失率。  
不建议先收紧股票宇宙；宇宙收紧解决不了逻辑恒删失，只会改变样本构成。修复后 G4 应按“可判定标签的真实删失”重算，limit contamination 单独列门，不能用仅计 9.9% 直接替代总体门径。

3. **允许启动 P0-05 正式变更流程，但允许改的是定义缺陷和基于训练期的重新预注册，不允许事后迎合 test 改阈值。**  
处理顺序为：冻结当前 P0-06 结果作为失败证据 → 出具协议勘误/变更单及版本号 → 只用 train 设计 G3 新定义与门槛、修复 G4 的 W/C 和删失分母 → 在任何 test 复看前锁定全部参数与验收门 → 全量重跑 P0-06。当前 test 已被查看过，因此修订后它只能作为诊断/旧测试集；若其结果参与过选择，最终确认须使用仍封存的 shadow，或另建未触碰的 OOS 段。

4. **Roadmap 影响：P0-07 继续挂起，不得带病放行。**  
新增一个短的 **P0-06A 协议修订与复跑包**：包含定义差异、训练期选择依据、标签分布、各删失原因分解、旧新结果对照及泄漏审计。P0-06A 通过 G1–G4 后再恢复 P0-07；下游所有依赖旧 bottoming 标签的缓存、特征和模型产物均须失效重建。

一句话结论：**不是坚持错误原门，也不是抬门迁就数据；应修复 H5 的确定性 bug，并用训练期重新收紧 bottoming 定义后预注册复跑，test/shadow 不参与调参。**